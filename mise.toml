# Pinned dev tools. swiftformat must match `--minversion` in
# apple/airbnb.swiftformat and the pinned download in .github/workflows/ci.yml —
# bump all three together or lint output drifts between local and CI.
[tools]
swiftformat = "0.63.0"

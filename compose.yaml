services:
  lurker:
    image: ghcr.io/lepinkainen/lurker:latest
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./data:/data
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
